import React, { useEffect, useState } from "react";
const API_KEY = import.meta.env.VITE_APP_API_KEY;


function CoinInfo({image,name,sample}){
    const[price,setPrice] = useState(null)
    useEffect(() => {

    }, [symbol]);
} 

export default CoinInfo